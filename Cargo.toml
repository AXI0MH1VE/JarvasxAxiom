[workspace]
members = [
    "sovereign-node",
    "sovereign-protocol",
    "sovereign-mesh",
    "sovereign-finance",
    "sovereign-core",
    "sovereign-runtime-wasm",
]
resolver = "2"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
